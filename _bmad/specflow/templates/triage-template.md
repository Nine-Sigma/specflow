# Triage: {{ISSUE_ID}}

**Issue:** {{TITLE}}
**Source:** {{SOURCE}}
**Started:** {{TIMESTAMP}}

## Classification

| Dimension | Value | Confidence | Source |
|-----------|-------|------------|--------|
| Type | {{TYPE}} | {{TYPE_CONFIDENCE}}% | {{TYPE_SOURCE}} |
| Size | {{SIZE}} | {{SIZE_CONFIDENCE}}% | {{SIZE_SOURCE}} |

**Pillars:** {{PILLARS}}

**Reasoning:** {{REASONING}}

{{#if NEEDS_LLM}}
> Note: Low confidence classification. PM agent should verify.
{{/if}}

{{#if OVERRIDE}}
> Override: User specified --type={{OVERRIDE_TYPE}} --size={{OVERRIDE_SIZE}}
{{/if}}

## Next Steps

Based on classification:

{{#if TYPE_BUG}}
1. Run `/sf:bug` workflow (testing focus)
{{/if}}
{{#if TYPE_FEATURE}}
1. Run `/sf:feature` workflow (full spec)
{{/if}}
{{#if TYPE_REFACTOR}}
1. Run `/sf:refactor` workflow (before/after comparison)
{{/if}}
{{#if TYPE_CHORE}}
1. Minimal ceremony - proceed directly
{{/if}}
{{#if TYPE_DOCS}}
1. Documentation workflow - no pillars required
{{/if}}

## Sync Status

- Last sync: {{LAST_SYNC}}
- Checkpoint: {{CHECKPOINT}}
- Conflicts: {{CONFLICTS}}

---

## Usage Notes

This file is auto-generated by `sf-work.sh` when starting work on an issue.
It captures the initial classification and can be referenced throughout the work.

**Template variables:**

| Variable | Description |
|----------|-------------|
| `ISSUE_ID` | Issue number (e.g., 123) |
| `TITLE` | Issue title |
| `SOURCE` | Issue source (github, jira, linear, local) |
| `TIMESTAMP` | When work started (ISO 8601) |
| `TYPE` | Classification type (bug, feature, refactor, chore, docs) |
| `TYPE_CONFIDENCE` | Type classification confidence (0-100) |
| `TYPE_SOURCE` | How type was determined (label, keyword, heuristic, override) |
| `SIZE` | Classification size (quick, standard, complex) |
| `SIZE_CONFIDENCE` | Size classification confidence (0-100) |
| `SIZE_SOURCE` | How size was determined (label, heuristic, override) |
| `PILLARS` | Comma-separated pillars (security, cost, testing) |
| `REASONING` | Classification reasoning |
| `NEEDS_LLM` | Whether PM agent should verify |
| `OVERRIDE` | Whether user provided overrides |
| `OVERRIDE_TYPE` | User-specified type override |
| `OVERRIDE_SIZE` | User-specified size override |
| `LAST_SYNC` | Last sync timestamp |
| `CHECKPOINT` | Last sync checkpoint |
| `CONFLICTS` | Conflict status |

**Conditional sections:**

- `{{#if NEEDS_LLM}}` - Shown when classification confidence is low
- `{{#if OVERRIDE}}` - Shown when user provided overrides
- `{{#if TYPE_*}}` - Shown based on classification type
